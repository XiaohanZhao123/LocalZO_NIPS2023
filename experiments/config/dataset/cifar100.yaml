name: cifar100
n_class: 100
batch_size: 32
constant_encoding: True
num_step: 50

get_dataset:
  _target_: experiments.dataset.cifar100.get_dataset
  root: /home/zxh/data/cifar100
  batch_size: ${dataset.batch_size}